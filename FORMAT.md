# 密文格式规范（兼容性契约）

> 本文档是加密/解密格式的**权威规范**。即使源码丢失，任何开发者按本文档重新实现，
> 配合用户密码即可解密本工具生成的所有密文（文本与文件两种）。
> 请勿在未同步更新本文档的情况下修改任何加密参数或格式。

## 0. 通用安全参数（文本与文件格式共用）

| 参数 | 值 | 说明 |
|---|---|---|
| 加密算法 | AES-256-GCM（`AES/GCM/NoPadding`） | GCM 自带完整性认证 |
| 密钥派生 | PBKDF2-HMAC-SHA256 | 从用户密码派生密钥 |
| **迭代次数** | **120,000** | ⚠️ 关键参数，必须一致，否则无法解密 |
| 密钥长度 | 256 位 | |
| 盐 | 随机 16 字节 | 每次加密独立随机 |
| IV / Nonce | 随机 12 字节 | 每次加密独立随机 |
| GCM 认证标签 | 128 位 | 附加在密文末尾 |
| 字符编码 | UTF-8 | 所有文本 |

派生流程：`key = PBKDF2_HMAC_SHA256(password, salt, 120000, 256bit)`

## 1. 文本密文格式（Base64，用于复制粘贴/文本框）

```
密文字符串 = Base64( salt(16B) + iv(12B) + GCM密文+tag )
```

- Base64 为 RFC 4648 标准字母表，**无换行、可选填充 `=`**（解码时忽略空白与 `=`）
- 解密步骤：
  1. Base64 解码得到字节串，长度必须 ≥ 16 + 12 + 1
  2. `salt = bytes[0..15]`，`iv = bytes[16..27]`，密文 = 剩余部分（含末尾 16B tag）
  3. 由 salt 派生密钥，用 iv 初始化 GCM，解密并校验 tag
  4. 失败（tag 不匹配）→ 密钥错误或密文被篡改，不输出任何内容

## 2. 文件密文格式（.bin，CYPT 格式）

```
文件布局（字节偏移）：
[0:4]    magic  "CYPT"（ASCII，识别文件类型）
[4:20]   salt   16 字节
[20:32]  iv     12 字节
[32:..]  GCM 密文流 + 末尾 128 位认证标签
```

- 加密：写 magic → salt → iv → 以 64KB 缓冲流式加密文件内容（`CipherOutputStream`），
  关闭流时自动追加 GCM 认证标签
- 解密：
  1. 读 4 字节校验 magic，非 `CYPT` → 不是本工具加密的文件
  2. 读 salt(16B) → iv(12B)
  3. 派生密钥，`CipherInputStream` 流式解密剩余全部字节，读到末尾自动校验 tag
  4. tag 不匹配 → 密钥错误或文件已损坏，不输出内容
- 流式处理：文件大小无上限，内存占用恒定（64KB 缓冲）

## 3. 解密兼容性检查清单（重写实现时逐项核对）

- [ ] AES-256-GCM，`GCMParameterSpec(128, iv)`
- [ ] PBKDF2-HMAC-SHA256，**迭代 120,000**，输出 256 位
- [ ] 盐 16B、IV 12B（随机，每次加密独立）
- [ ] 文本：Base64(盐+IV+密文+tag) 拼接顺序
- [ ] 文件：`CYPT` magic + 盐 + IV + 密文流（tag 在末尾）
- [ ] 失败一律不输出明文（GCM 认证保证）

## 4. 密钥保管提示

- 密码丢失或错误 → **永远无法解密**（无后门、无备份机制，这是有意设计）
- 迭代次数等参数公开无安全风险（PBKDF2 抗暴力破解不依赖参数保密），
  文档化是为了保证格式可重建
